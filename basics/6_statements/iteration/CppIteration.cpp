#include <string.h>
#include <stdio.h>
#include <istream>

// while 重复执行语句，直到表达式计算结果为零
// 1. 表达式必须是整型类型、指针类型或可以明确转换为整型或指针类型的类类型
// 2. 执行语句正文中的 break、goto 或 return 时，while 循环也可以终止
// 3. continue 将控制转移到 while 循环的下一次迭代

int main() {


    return 0;
}

C++ Identifiers 标识符号可以下划线开头
A-Z or a-z or underscore (_) followed letters, underscores, and digits

标识符的第一个字符必须是字母字符(大写、小写或带下划线(_)的字母)
标识符区分大小写，且不能与关键字有完全相同的拼写和大小写

TODO. C++ 源码实现所预留的标识符
在标识符中两个顺序下划线字符(__)或在单个前导下划线后跟一个大写字母(_F)的用法是专为所有范围的C++实现保留的
由于当前或将来的保留标识符可能发生冲突，因此应避免对文件范围的名称使用一个前导下划线后跟小写字母

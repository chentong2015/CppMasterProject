#ifndef MyForm_H
#define MyForm_H

/* 
#include <QLabel>
#include <QWidget>
#include <QLineEdit>
#include <QPushButton>

class MyForm : public QWidget {
public:
    QLabel *label; // 显示在界面的标签
    QLineEdit *lineEdit; // 页面的编辑输入框
    QPushButton *button; // 用户点击的按钮

    // MyFrom class's constructor with parent value 0
    // form object will be the parent for all the widget objects inside it
    // simply QLabel, QPushbutton.. will be children of MyForm object when created..
    MyForm(QWidget *parent = 0);
private slots:
    // A slot that will set the value of label to whatever was written inside QLineEdit.
    void addTextToLabel();
}; */

#endif

1. Enum 枚举类型：
   用于约束值的可取值范围，进行限定

2. Struct 结构体类型：
   结构类型是用户自定义的复合类型，它由可具有不同类型的字段或成员构成s

3. Tuple 元祖类型：
   CPP默认提供的模版类型，包含任意数量不同类型的数据
   template <class ..._Tp>
   class tuple {}

4. Union 联合类型：
   一种自定义的类型，其中所有成员都共享同一个内存位置，用于节省内存
   只允许初始化其中一种数据，使用时只有一种数据
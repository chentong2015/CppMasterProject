Enum 枚举类型：用于约束值的可取值范围，进行限定

Struct 结构体类型：
  一种自定义的数据结构，将不同数据类型包装到一个自定义的结构中
  允许初始化其中任意指定的数据

Tuple 元祖类型：CPP默认提供的模版类型，包含任意数量不同类型的数据
  template <class ..._Tp>
  class _LIBCPP_TEMPLATE_VIS tuple {
  }

Union 联合类型：
  一种自定义的类型，其中所有成员都共享同一个内存位置，用于节省内存
  只允许初始化其中一种数据，同时只能使用一种数据
指针是一个变量，可以存储一个对象的内存地址
- 它将对象的地址存储在内存中，并用于访问该对象
- 未分配值的指针包含随机数据，在x64机器上大小为8个字节
- 非常量指针可以递增或递减，以指向内存中的另一个位置

1. 在堆上分配新对象
2. 将函数传递给其他函数
3. 循环访问数组或其他数据结构中的元素

TODO. 使用指针注意事项：
- 当不再需要"堆分配"的对象时，必须使用拥有指针(或其副本)显式释放该对象，避免内存泄漏
-

TODO. 三种声明(创建)指针的方式，推荐第一种声明方式
- type* p;
- type *p;
- type * p;
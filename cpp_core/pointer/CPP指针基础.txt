指针: 一个存储对象的内存地址的变量
- 它将对象的地址存储在内存中，并用于访问该对象
- 未分配值的指针包含随机数据，在x64机器上大小为8个字节
- 非常量指针可递增或递减移动，以指向内存中的另一个位置

TODO. 三种声明(创建)指针的方式
- type* p;
- type *p;
- type * p;

TODO. 为什么要使用指针? 指针的主要用途
1. 在堆上分配新对象：当程序在内存中的“堆”上分配对象时，它会以指针的形式接收该对象的地址
2. 将函数传递给其他函数：lambda表达式
3. 循环访问数组或其他数据结构中的元素：指针可以判断nullptr空